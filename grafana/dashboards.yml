apiVersion: 1

providers:
  - name: 'Prefect Pipeline Dashboards'
    orgId: 1
    folder: 'Prefect Pipeline'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: true